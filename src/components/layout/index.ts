/**
 * Layout Components Index
 * Motion Connect HR System Design System
 */

// Layout Components
export { default as Container, PageContainer, SectionContainer, DashboardContainer } from './Container';

// Re-export types for convenience
export type { 
  ContainerProps
} from '@/lib/design-system/types';